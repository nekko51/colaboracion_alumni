\section{Support Vector Machine}

We intend to back up the humanness of the chain outputted by the simulated annealing algorithm with this binary-output method.

The main point of an SVM (abbreviation of Support Vector Machine) is to transform our data space into some other one in such a way that we can separate the data points with a simple hyperplane, a linear classifier.

We will represent each point with a $n$-dimensional vector, where $n$ is the number of data points. The function that gives us those coordinates is called the kernel.

\subsection{The kernel}

Our kernel, $\operatorname k:\mathcal C\times\mathcal C\to \mathbb R$, will be the Gaussian radial basis function: $$\operatorname k(\textbf x_i, \textbf x_j)=\exp \left( 
    -\gamma \left\| \textbf x_i -\textbf x_j \right\|^2 
\right),\ \gamma>0,$$ where $\{\textbf x_i\}$ are the data points.

After applying the kernel we have a matrix $M\in\mathbb R^{n\times n}$, the Gram matrix, with the coordinates of our data points in the new space. That will be: $[M]_{i,j}=\operatorname k(\textbf{x}_i,\textbf{x}_j)$.

We also have available four other standard kernel functions:\futnotetext\footnotetext{These constants ($298$ and $596=2\cdot 298$) that are scattered around the functions seem to be arbitrary, but the written functions are already optimized for our use case: they are normalized and centered so that every output is positive and does not surpass 1.}

\begin{multicols}{2}
\begin{itemize}
    \item Sigmoid function:
    \begin{equation*}
        \operatorname k(\textbf{x} _i,\textbf{x} _j)=\tanh \frac{
            \left\langle \textbf{x} _i| \textbf{x} _j \right\rangle
        }{
            298
        }
    \end{equation*}

    \item Tanimoto function:
    \begin{equation*}
        \operatorname k(\textbf{x} _i,\textbf{x} _j)=\frac{
            \left\langle \textbf{x} _i| \textbf{x} _j \right\rangle
        }{596 - \left\langle \textbf{x} _i| \textbf{x} _j \right\rangle}
    \end{equation*}
    
    \item Linear function: \begin{equation*}
        \operatorname k(\textbf{x} _i,\textbf{x} _j)=
        \frac{\left\langle \textbf{x} _i| \textbf{x} _j \right\rangle}{298}
    \end{equation*}

    \item Cauchy function:
    \begin{equation*}
        \operatorname k(\textbf{x} _i,\textbf{x} _j)=\frac{
            298
        }{298 + \left\| \textbf{x}_i-\textbf{x}_j   \right\|^2 }
    \end{equation*}
\end{itemize}
\end{multicols}

The Gram matrix and the kernel are the tools we need in order to craft the decision function.

\subsection{Decision function}

Using the lagrange multipliers method for minimization\futnotetext\footnotetext{\hyperref[sec:svm_formula]{See appendix for full derivation.}}, we get the decision function, $f:\mathcal C\to\mathbb R$:
$$
f(\textbf x) = b + \sum_{i=1}^n\lambda_i s_i \operatorname k(\textbf x_i,\textbf x),
$$
where $\lambda_i$ are the lagrange multipliers obtained through said method, $s_i$ is the \textit{tag} of the $i$-th data point (may be $+1$, when human, or $-1$, when murine) and $b$ is the bias, obtained as follows:
$$
b=s_p-\sum_{i=1}^n \lambda_i s_i \operatorname k(\textbf x_i, \textbf x_p),
$$
where the $p$-th data point is a support one, that is, the closest to the plane (or any of them) and such that $\lambda_p>0$.


\newpage
\section{SVM results}

Using the \texttt{learn\_human}, \texttt{learn\_mouse}, \texttt{test\_human}, and \texttt{test\_mouse} sets for training and testing, we get the following results. In this histogram, we show the distribution of the sign-corrected certainty degree (positive when correct and negative when failed tagging):

\begin{figure}[H]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            ybar,
            bar width=4.5pt,
            width=0.75\textwidth,
            height=0.38\textwidth,
            xlabel={Sign-corrected certainty degree $s \cdot f(\textbf x)$},
            ylabel={Count},
            xmin=-1.5, xmax=3.5,
            ymin=0,
            grid=both,
            grid style={line width=0.3pt, draw=gray!30},
            tick label style={font=\small},
            label style={font=\small},
            title style={font=\small},
            title={Distribution of certainty degree on the initial test set ($n=2767$)},
            legend style={at={(0.95,0.92)}, anchor=north east, font=\small, draw=none, fill=none},
        ]
            \addplot[fill=blue!50!white, draw=blue!70!black] table[x=bin_mid, y=total_count, col sep=tab] {../results/svm/plots/good_rbf_hist.txt};
            \addlegendentry{All chains}
            
            \draw[red!80!black, thick, dashed] (axis cs:0, 0) -- (axis cs:0, 450);
        \end{axis}
    \end{tikzpicture}
    \caption{Count with respect to sign-corrected certainty degree on the initial dataset.}
    \label{fig:goodRBFhist}
\end{figure}


This gets us to the confusion matrix:

\noindent\begin{minipage}{.45\textwidth}\setlength{\parindent}{15pt}
\begin{table}[H]
    \centering
    \renewcommand{\arraystretch}{1.5}
    \begin{tabular}{ccc} \cline{2-3}
        & Human & Murine \\ \hline
    Tagged as human    & 1360 & 41 \\ \hline
    Tagged as murine    & 28 & 1338 \\ \hline
    \end{tabular}
\end{table}
\end{minipage}\hfill\noindent\begin{minipage}{.45\textwidth}\setlength{\parindent}{15pt}
\begin{table}[H]
    \centering
    \renewcommand{\arraystretch}{1.5}
    \begin{tabular}{ccc} \cline{2-3}
    Overall percentages    & Human & Murine \\ \hline
    Tagged as human    & $49.15\%$ & $1.48\%$ \\ \hline
    Tagged as murine    & $1.01\%$ & $48.36\%$ \\ \hline
    \end{tabular}
\end{table}
\end{minipage}
\vspace\baselineskip

Counting human as positive, this gets us to a F1\futnotetext\footnotetext{$\text{F1}=\frac{2\cdot \text{TP} }{2\cdot \text{TP}+\text{FN}+\text{FP}   }$} of $\sim 0.9753$\futnotetext\footnotetext{Precisely: $\text{F1}_{\text{H}}=0.975259949802797$.}. On the other hand, being murine the positives, we have an F1 of $\sim 0.9749$\futnotetext\footnotetext{Precisely: $\text{F1}_{\text{M}}=0.974863387978142$.}. This takes us to an overall accuracy\futnotetext\footnotetext{$\text{Acc}=\frac{\text{TP}+\text{TN}  }{\text{TP}+\text{TN}+\text{FP}+\text{FN}    } $} of $\sim 0.9751$\futnotetext\footnotetext{Precisely: $\text{Acc}=  0.975063245392121$.}.

However, those tests were taken with a completely heuristic upper limit, we will call $\lambda_\text{t} $, for the $\lambda$ vector. Since we cannot determine $\lambda_\text{t} $ analytically, we must adjust it as a hyperparameter with a validation data set.

\newpage
\subsection{Validation}
We have merged all data sets and split them into \texttt{learn}, \texttt{validation}, and \texttt{test}, with $70\%$, $15\%$, and $15\%$ of the chains respectively.

Through this method we get an optimal value for $\lambda_\text{t}=10.0$, having tried with powers of $10$ from $0.01$ to $1000$. Using that hyperparameter, we get the following confusion matrix

\noindent\begin{minipage}{.45\textwidth}\setlength{\parindent}{15pt}
\begin{table}[H]
    \centering
    \renewcommand{\arraystretch}{1.5}
    \begin{tabular}{ccc} \cline{2-3}
        & Human & Murine \\ \hline
    Tagged as human    & 404 & 4 \\ \hline
    Tagged as murine    & 6 & 254 \\ \hline
    \end{tabular}
\end{table}
\end{minipage}\hfill\noindent\begin{minipage}{.45\textwidth}\setlength{\parindent}{15pt}
\begin{table}[H]
    \centering
    \renewcommand{\arraystretch}{1.5}
    \begin{tabular}{ccc} \cline{2-3}
    Overall percentages    & Human & Murine \\ \hline
    Tagged as human    & $60.48\%$ & $0.60\%$ \\ \hline
    Tagged as murine    & $0.90\%$ & $38.02\%$ \\ \hline
    \end{tabular}
\end{table}
\end{minipage}
\vspace\baselineskip


And F1 values and accuracy:
\begin{equation*}
    \text{F1}_\text{H} = 0.9878\futnotemath\footnotetext{Precisely: $\text{F1}_\text{H} = 0.987775061124694$.}  \qquad
    \text{F1}_\text{M} = 0.9788\futnotemath\footnotetext{Precisely: $\text{F1}_\text{M} =0.978805394990366$.}  \qquad
    \text{Acc} = 0.9836\futnotemath\footnotetext{Precisely: $\text{Acc}=0.98355754857997 $} 
\end{equation*}

The following histogram shows the distribution of the sign-corrected certainty degree, as before positive when correct and negative when incorrect tagging:
\begin{figure}[H]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            ybar,
            bar width=4.5pt,
            width=0.75\textwidth,
            height=0.38\textwidth,
            xlabel={Sign-corrected certainty degree $s \cdot f(\textbf x)$},
            ylabel={Count},
            xmin=-1.0, xmax=3.5,
            ymin=0,
            grid=both,
            grid style={line width=0.3pt, draw=gray!30},
            tick label style={font=\small},
            label style={font=\small},
            title style={font=\small},
            title={Distribution of certainty degree on the self-split test dataset ($n=668$)},
            legend style={at={(0.95,0.92)}, anchor=north east, font=\small, draw=none, fill=none},
        ]
            \addplot[fill=blue!50!white, draw=blue!70!black] table[x=bin_mid, y=total_count, col sep=tab] {../results/svm/plots/test_rbf_hist.txt};
            \addlegendentry{All chains ($n=668$)}
            
            \draw[red!80!black, thick, dashed] (axis cs:0, 0) -- (axis cs:0, 110);
        \end{axis}
    \end{tikzpicture}
    \caption{Count with respect to sign-corrected certainty degree using self-split learn, validation, and test datasets.}
    \label{fig:test_hist}
\end{figure}

\newpage
\subsection{Cross-validation of standard kernels}
And with every standard kernel we presented, we obtain similar results:

\begin{figure}[H]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            width=0.48\textwidth,
            height=0.3\textwidth,
            xlabel={Sign-corrected certainty degree $s \cdot f(\textbf x)$},
            ylabel={Density},
            xmin=-0.8, xmax=2.8,
            ymin=0, ymax=1.55,
            grid=both,
            grid style={line width=0.3pt, draw=gray!30},
            tick label style={font=\footnotesize},
            label style={font=\small},
            title style={font=\small},
            title={Distance-based kernels},
            legend style={at={(0.05,0.95)}, anchor=north west, font=\footnotesize, draw=none, fill=none},
        ]
            \addplot[color=green!60!black, thick, smooth] table[x=x, y=Gaussian, col sep=tab] {../results/svm/plots/density_families.txt};
            \addlegendentry{Gaussian}

            \addplot[color=blue!70!black, thick, smooth] table[x=x, y=Cauchy, col sep=tab] {../results/svm/plots/density_families.txt};
            \addlegendentry{Cauchy}

            \addplot[color=orange!80!black, thick, smooth] table[x=x, y=Tanimoto, col sep=tab] {../results/svm/plots/density_families.txt};
            \addlegendentry{Tanimoto}
        \end{axis}
    \end{tikzpicture}
    \hfill
    \begin{tikzpicture}
        \begin{axis}[
            width=0.48\textwidth,
            height=0.3\textwidth,
            xlabel={Sign-corrected certainty degree $s \cdot f(\textbf x)$},
            ylabel={Density},
            xmin=-0.8, xmax=2.8,
            ymin=0, ymax=1.55,
            grid=both,
            grid style={line width=0.3pt, draw=gray!30},
            tick label style={font=\footnotesize},
            label style={font=\small},
            title style={font=\small},
            title={Inner product-based kernels},
            legend style={at={(0.05,0.95)}, anchor=north west, font=\footnotesize, draw=none, fill=none},
        ]
            \addplot[color=teal!80!black, thick, smooth] table[x=x, y=Linear, col sep=tab] {../results/svm/plots/density_families.txt};
            \addlegendentry{Linear}

            \addplot[color=purple!80!black, thick, smooth] table[x=x, y=Sigmoid, col sep=tab] {../results/svm/plots/density_families.txt};
            \addlegendentry{Sigmoid}
        \end{axis}
    \end{tikzpicture}
    \caption{Density distributions of sign-corrected certainty degrees $s \cdot f(\textbf x)$ on the test dataset for distance-based kernels (left) and inner product-based kernels (right). Values greater than zero ($s \cdot f > 0$) are correct classifications, and values below zero ($s \cdot f < 0$) are errors.}
    \label{fig:family_histograms}
\end{figure}

To see how their predictions relate to each other, we plot the scatterplot matrix of decision function outputs $f(\textbf x)$ in Figure \ref{fig:splom5}.

\input{images/scatterplot_mat.tex}

\newpage
Here we have the correlation matrix for the five standard kernels:\futnotetext
\footnotetext{The correlations were expected, to a certain level, because of how similar the functions used as kernel are to each other. \hyperref[sec:svm_comparison]{See appendix for detail.}}

\input{sections/correlation_matrix.tex}

As we can see from both the scatterplot matrix and the correlation heatmap, the five standard kernels are very correlated ($r > 0.96$ for all pairs, and $r \approx 0.998$ between Linear and Sigmoid). Because these functions are smooth across the whole range $[0, 1]$, they apply a uniform scale and do not focus on the narrow similarity interval where antibodies switch from murine to human.

\newpage
\subsection{Custom piecewise kernels}
To focus on this boundary region, we define a piecewise linear kernel with a ramp of width $W$ centered at $C$:

\input{images/pieceuise.tex}

By choosing a narrow width ($W = 0.08$) and placing the center $C$ in the transition region ($C \in \{0.78, 0.80, 0.82, 0.84\}$), small differences between sequences near the threshold produce large changes in the kernel, while sequences far from the boundary stay in constant regions.

To measure both classification accuracy and margin certainty in a single score between 0 and 1, we define the Quadratic Certainty Score $\mathcal Q$:
\begin{equation*}
    \mathcal Q = 1 - \frac{1}{n}\sum_{i=1}^n \left(1 - \frac{1}{1 + e^{-s_i f(\textbf x_i)}}\right)^2 \in [0, 1]
\end{equation*}
where $s_i \in \{-1, +1\}$ is the true tag and $f(\textbf x_i)$ is the decision function output. Correct predictions with high margin give values close to $1.0$, predictions on the boundary give $0.75$, and wrong predictions with high margin give values close to $0.0$.

\newpage
Evaluating these custom kernels on the test dataset gives the results shown in Figure \ref{fig:piecewise_metrics} (\hyperref[sec:piecewise_results_table]{see appendix for the full table}):

\input{images/metrix1.tex}

The piecewise kernels with $C=0.82$ and $C=0.84$ reach an $\text{F1}_\text{H}$ of $0.9915$ and $98.95\%$ accuracy, higher than any of the five standard kernels.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}
        \begin{axis}[
            width=0.48\textwidth,
            height=0.42\textwidth,
            xlabel={Gaussian ($f_g$)},
            ylabel={Piecewise $C=0.82$ ($f_{pw}$)},
            grid=both,
            grid style={line width=0.3pt, draw=gray!30},
            tick label style={font=\footnotesize},
            label style={font=\small},
            title style={font=\small},
            title={Gaussian vs. Piecewise $C=0.82$},
            legend style={at={(0.05,0.92)}, anchor=north west, font=\footnotesize, draw=none, fill=none},
        ]
            \addplot[only marks, mark=*, mark size=1.0pt, color=blue!60!black, opacity=0.6] table[x=g, y=pw82_08, col sep=tab] {../results/svm/plots/scatter_pw_human.txt};
            \addlegendentry{Human}

            \addplot[only marks, mark=*, mark size=1.0pt, color=magenta!80!black, opacity=0.6] table[x=g, y=pw82_08, col sep=tab] {../results/svm/plots/scatter_pw_murine.txt};
            \addlegendentry{Murine}
        \end{axis}
    \end{tikzpicture}
    \hfill
    \begin{tikzpicture}
        \begin{axis}[
            width=0.48\textwidth,
            height=0.42\textwidth,
            xlabel={Piecewise $C=0.80$ ($f_{80}$)},
            ylabel={Piecewise $C=0.84$ ($f_{84}$)},
            grid=both,
            grid style={line width=0.3pt, draw=gray!30},
            tick label style={font=\footnotesize},
            label style={font=\small},
            title style={font=\small},
            title={Piecewise $C=0.80$ vs. $C=0.84$},
            legend style={at={(0.05,0.92)}, anchor=north west, font=\footnotesize, draw=none, fill=none},
        ]
            \addplot[only marks, mark=*, mark size=1.0pt, color=blue!60!black, opacity=0.6] table[x=pw80_08, y=pw84_08, col sep=tab] {../results/svm/plots/scatter_pw_human.txt};
            \addlegendentry{Human}

            \addplot[only marks, mark=*, mark size=1.0pt, color=magenta!80!black, opacity=0.6] table[x=pw80_08, y=pw84_08, col sep=tab] {../results/svm/plots/scatter_pw_murine.txt};
            \addlegendentry{Murine}
        \end{axis}
    \end{tikzpicture}
    \caption{Scatterplots of decision output for custom piecewise kernels compared against standard Gaussian (left) and between two fine-tuned piecewise centers (right).}
    \label{fig:piecewise_scatter}
\end{figure}

We now have 10 different kernel tests: 5 standard global kernels and 5 piecewise kernels. In the next section, we train a logistic regression model to find the best linear combination of all 10 kernel outputs.
